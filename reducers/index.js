import {
    DROP_BALL
} from '../actions'

function entries(state = {}, action) {
    switch (action.type) {
        case DROP_BALL:
        {
            return state
        }
        default:
        {
            return state
        }
    }
}

export default entries